class WorkoutResult {
  final String name;
  final int totalMinutes;
  final int totalCalories;

  WorkoutResult({
    required this.name,
    required this.totalMinutes,
    required this.totalCalories,
  });
}
